<Window x:Class="CourterClient.Gui.CalendarWindow.CalendarView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" 
        AllowsTransparency="True"
        Background="Transparent"
        Title="Kalender" 
        Height="300" 
        Width="300">
    <Grid>
        <Image Source="/Logos/SchließenGrau.png" Grid.Column="1" Panel.ZIndex="1" MouseUp="Image_MouseUp" Style="{StaticResource imgClose}"/>
        <Border CornerRadius="20 20 20 20" MouseDown="Border_MouseDown" Background="White">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="0.3*"/>
                </Grid.RowDefinitions>
                <Calendar x:Name="Calendar" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" Background="Transparent" />
                <Button Grid.Row="1" Content="Übernehmen" Style="{StaticResource mainbutton}" Margin="5"/>
            </Grid>
        </Border>
    </Grid>
</Window>
